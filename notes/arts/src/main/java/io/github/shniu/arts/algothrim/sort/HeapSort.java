package io.github.shniu.arts.algothrim.sort;

/**
 * 堆排序
 * 实现一个二叉堆，二叉堆是一个完全二叉数，适合使用数组来存储
 */
public class HeapSort implements Sortable {

    @Override
    public void sort(Comparable[] arr) {
        if (arr.length <= 1) return;

        // 构建小顶堆

        // 依次取堆顶元素
    }

    // 堆化
    private void heapify() {

    }
}
