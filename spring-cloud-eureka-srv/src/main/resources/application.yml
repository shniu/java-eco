server:
  port: 8761

spring:
  application:
    name: coffee-eureka-srv

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka
